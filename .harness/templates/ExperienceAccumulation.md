## 复利沉淀任务：{问题标题}

### 1. 问题记录
- **输入/刺激**：触发问题的具体输入
- **实际行为**：发生了什么不期望的结果
- **期望行为**：应该发生什么

### 2. 根因分析
- 是知识缺失？写入 `GLOSSARY.md`
- 是模式缺失？写入 `PATTERNS.md`
- 是架构约束缺失？更新 `ARCHITECTURE.md`
- 是审查规则缺失？更新 `.harness/agents/REVIEWER.md`

### 3. 环境固化
- [ ] 在 `PATTERNS.md` 中记录正确模式
- [ ] 在 `PATTERNS.md` 中记录对应反模式
- [ ] 如可自动检测，补充 lint、测试或检查脚本
- [ ] 如有审查需求，更新 `.harness/agents/REVIEWER.md`
- [ ] 在 `implementation.md` 记录知识沉淀和文档防腐状态

### 4. 决策日志
- **决策**：为什么选择这个方案
- **备选**：考虑过哪些方案以及放弃原因
- **后果**：由该决策带来的新约束

### 5. 防腐证据
- **目标文档**：写入了哪里
- **引用检查**：示例路径、设计文档、agent 文档是否可达
- **任务状态**：`待落库` / `已落库` / `待确认` / `已阻塞` 是否已闭合
